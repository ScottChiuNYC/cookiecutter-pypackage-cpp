{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch (Windows)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/windows-vcpkg/tests/cpp/Debug/{{ cookiecutter.module_name }}_core_test.exe",
            "stopAtEntry": false,
            "preLaunchTask": "CMake: build (Debug)",
            "cwd": "${workspaceFolder}"
        }
    ]
}
